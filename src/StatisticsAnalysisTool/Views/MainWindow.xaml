<Window x:Class="StatisticsAnalysisTool.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:fa5="http://schemas.fontawesome.com/icons/"
        xmlns:userControls="clr-namespace:StatisticsAnalysisTool.UserControls"
        xmlns:viewModels="clr-namespace:StatisticsAnalysisTool.ViewModels"
        xmlns:glob="clr-namespace:System.Globalization;assembly=System.Runtime"
        mc:Ignorable="d"
        d:DesignHeight="850" d:DesignWidth="1100"
        Title="AlbionOnline - DataVault"
        Closing="MainWindow_OnClosing"
        d:DataContext="{d:DesignInstance Type=viewModels:MainWindowViewModel}"
        Style="{StaticResource MainWindowStyle}">

    <Grid Style="{StaticResource MainGridStyle}">
        <Grid Style="{StaticResource BackgroundImageGridStyle}" />

        <Grid ZIndex="-1" Height="60" VerticalAlignment="Top" MouseDown="Hotbar_MouseDown" MouseLeftButtonDown="Grid_MouseLeftButtonDown">
            <Grid Background="{StaticResource SolidColorBrush.Panel}" Opacity="0.92" />
            <Border VerticalAlignment="Bottom" Height="1" Background="{StaticResource SolidColorBrush.Hover}" />
            <Button IsEnabled="{Binding IsCloseButtonActive}" Style="{StaticResource WindowButtonStyle}" Content="r" Margin="-2,0,0,0" Click="CloseButton_Click" />
            <Button x:Name="MaximizedButton" Style="{StaticResource WindowButtonStyle}" Content="1" Margin="0,0,28,0" Click="MaximizeButton_Click" />
            <Button x:Name="MinimizeButton" Style="{StaticResource WindowButtonStyle}" Content="0" Margin="0,0,58,0" Click="MinimizeButton_Click" />

            <!-- Logo + title -->
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="14,0,0,0" IsHitTestVisible="False">
                <Image Style="{StaticResource Image.ToolIcon}" Margin="0,0,8,0" VerticalAlignment="Center" HorizontalAlignment="Left" Width="26" Height="22" />
                <Label Content="AlbionOnline - DATAVAULT" Style="{StaticResource MainWindow.Title}" FontSize="14" VerticalContentAlignment="Center" Padding="0" />
                <Grid Visibility="{Binding UnsupportedOsVisibility, FallbackValue=Collapsed}"
                      Height="18" Width="18" Margin="6,0,0,0" VerticalAlignment="Center" ToolTip="{Binding Translation.UnsupportedOs, FallbackValue='UNSUPPORTED_OS'}">
                    <fa5:ImageAwesome Icon="Solid_ExclamationTriangle" Foreground="{StaticResource SolidColorBrush.Accent.Red.3}" />
                </Grid>
                <Label Content="- DEBUG -" Style="{StaticResource MainWindow.Title}" FontSize="12" Foreground="{StaticResource SolidColorBrush.Accent.Red.3}"
                       Visibility="{Binding DebugModeVisibility}" VerticalContentAlignment="Center" />
                <Grid Style="{StaticResource LoadingIconGridStyle}" VerticalAlignment="Center" Margin="6,0,0,0" Height="16" Width="16">
                    <fa5:ImageAwesome Visibility="{Binding LoadIconVisibility}" Style="{StaticResource LoadingIconStyle}" />
                </Grid>
            </StackPanel>

            <!-- Global search: reuses the same SearchText/ItemsView filter as the "Buscar objeto" screen -->
            <Grid HorizontalAlignment="Center" VerticalAlignment="Center" Width="340" Height="34" IsHitTestVisible="True">
                <TextBox x:Name="HeaderSearchTextBox" Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                         IsEnabled="{Binding IsTxtSearchEnabled, FallbackValue=false}" Height="34" Padding="30,6,10,6"
                         GotFocus="HeaderSearchTextBox_GotFocus" />
                <fa5:ImageAwesome Icon="Solid_Search" Height="12" Width="12" Margin="12,0,0,0"
                                  HorizontalAlignment="Left" VerticalAlignment="Center"
                                  Foreground="{StaticResource SolidColorBrush.TextSecondary}" IsHitTestVisible="False" />
                <TextBlock Text="{Binding Translation.SearchItemPlaceholder, FallbackValue='Search item...'}"
                           Foreground="{StaticResource SolidColorBrush.TextSecondary}"
                           Margin="32,0,0,0" VerticalAlignment="Center" HorizontalAlignment="Left"
                           IsHitTestVisible="False" Focusable="False">
                    <TextBlock.Style>
                        <Style TargetType="{x:Type TextBlock}">
                            <Setter Property="Visibility" Value="Collapsed" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding SearchText}" Value="">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding SearchText}" Value="{x:Null}">
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>

            <!-- Session / Party / Settings cluster -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,95,0">
                <Border Background="{StaticResource SolidColorBrush.Card}" CornerRadius="6" Padding="10,5" Margin="0,0,10,0"
                        Cursor="Hand" PreviewMouseDown="TatsDropDownOpenClose_PreviewMouseDown" ToolTip="{Binding Translation.Status, FallbackValue=STATUS}">
                    <StackPanel Orientation="Horizontal">
                        <fa5:ImageAwesome Icon="Solid_Clock" Width="12" Height="12" Foreground="{StaticResource SolidColorBrush.TextSecondary}" Margin="0,0,6,0" VerticalAlignment="Center" />
                        <Label Content="{Binding MainTrackerTimer, FallbackValue=000:00:00}" Padding="0" FontSize="12" VerticalContentAlignment="Center" Foreground="{StaticResource SolidColorBrush.Text.1}" />
                    </StackPanel>
                </Border>

                <StackPanel Orientation="Horizontal" Margin="0,0,14,0" VerticalAlignment="Center">
                    <Grid Height="20" Width="20" Margin="0,0,5,0" VerticalAlignment="Center" PreviewMouseDown="CopyPartyToClipboard_PreviewMouseDown">
                        <fa5:ImageAwesome Icon="Solid_Users" Foreground="{StaticResource SolidColorBrush.Text.1}" />
                    </Grid>
                    <Label Content="{Binding PartyMemberNumber, FallbackValue=0}" Padding="0" VerticalContentAlignment="Center" Foreground="{StaticResource SolidColorBrush.Text.1}" />
                    <ItemsControl ItemsSource="{Binding PartyMemberCircles}" Style="{StaticResource PartyMember.ItemsControl}" />
                </StackPanel>

                <Grid Height="24" Width="24" VerticalAlignment="Center" Cursor="Hand" MouseUp="SettingsShortcut_MouseUp" ToolTip="{Binding Translation.Settings, FallbackValue=SETTINGS}">
                    <fa5:ImageAwesome Icon="Solid_Cog" Foreground="{StaticResource SolidColorBrush.TextSecondary}" />
                </Grid>
            </StackPanel>

            <ProgressBar Margin="0,59,0,0" Height="1" Minimum="0" Maximum="100" Value="{Binding ToolTaskProgressBarValue, FallbackValue=0}" />
        </Grid>

        <!-- Stats drop down -->
        <Grid ZIndex="3" Height="90" MaxWidth="500" Margin="0,60,5,0" HorizontalAlignment="Center" VerticalAlignment="Top"
              Background="{StaticResource SolidColorBrush.Background.3}" Visibility="{Binding StatsDropDownVisibility, FallbackValue=Visible}">
            <Border BorderBrush="{StaticResource SolidColorBrush.Text.7}" BorderThickness="1">
                <DockPanel HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10">
                    <StackPanel Orientation="Vertical" Margin="10,0,10,0">
                        <Image Source="../Resources/fame.png" Width="22" Height="22" ToolTip="{Binding Translation.Fame, FallbackValue=FAME}" />
                        <TextBlock Text="{Binding DashboardBindings.TotalGainedFameInSession, Converter={StaticResource ShortNumberConverter}, ConverterCulture={x:Static glob:CultureInfo.CurrentCulture}, FallbackValue=0}" 
                                       FontSize="16" Foreground="{StaticResource SolidColorBrush.Text.1}" HorizontalAlignment="Center" />
                        <TextBlock FontSize="16" Text="{Binding DashboardBindings.FamePerHour, Converter={StaticResource ValuePerHourConverter}, FallbackValue=0 /h}"
                           Foreground="{StaticResource SolidColorBrush.Text.1}" HorizontalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Margin="10,0,10,0">
                        <Image Source="../Resources/silver.png" Width="22" Height="22" ToolTip="{Binding Translation.Silver, FallbackValue=SILVER}" />
                        <TextBlock Text="{Binding DashboardBindings.TotalGainedSilverInSession, Converter={StaticResource ShortNumberConverter}, ConverterCulture={x:Static glob:CultureInfo.CurrentCulture}, FallbackValue=0}" 
                                       FontSize="16" Foreground="{StaticResource SolidColorBrush.Text.1}" HorizontalAlignment="Center" />
                        <TextBlock FontSize="16" Text="{Binding DashboardBindings.SilverPerHour, Converter={StaticResource ValuePerHourConverter}, FallbackValue=0 /h}"
                           Foreground="{StaticResource SolidColorBrush.Text.1}" HorizontalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Margin="10,0,10,0">
                        <Image Source="../Resources/respec.png" Width="22" Height="22" ToolTip="{Binding Translation.ReSpec, FallbackValue=RESPEC}" />
                        <TextBlock Text="{Binding DashboardBindings.TotalGainedReSpecPointsInSession, Converter={StaticResource ShortNumberConverter}, ConverterCulture={x:Static glob:CultureInfo.CurrentCulture}, FallbackValue=0}" 
                                       FontSize="16" Foreground="{StaticResource SolidColorBrush.Text.1}" HorizontalAlignment="Center" />
                        <TextBlock FontSize="16" Text="{Binding DashboardBindings.ReSpecPointsPerHour, Converter={StaticResource ValuePerHourConverter}, FallbackValue=0 /h}"
                           Foreground="{StaticResource SolidColorBrush.Text.1}" HorizontalAlignment="Center"/>
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Margin="10,0,10,0">
                        <Image Source="../Resources/might.png" Width="22" Height="22" ToolTip="{Binding Translation.Might, FallbackValue=MIGHT}" />
                        <TextBlock Text="{Binding DashboardBindings.TotalGainedMightInSession, Converter={StaticResource ShortNumberConverter}, ConverterCulture={x:Static glob:CultureInfo.CurrentCulture}, FallbackValue=0}" 
                                       FontSize="16" Foreground="{StaticResource SolidColorBrush.Text.1}" HorizontalAlignment="Center" />
                        <TextBlock FontSize="16" Text="{Binding DashboardBindings.MightPerHour, Converter={StaticResource ValuePerHourConverter}, FallbackValue=0 /h}"
                           Foreground="{StaticResource SolidColorBrush.Text.1}" HorizontalAlignment="Center" />
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Margin="10,0,10,0">
                        <Image Source="../Resources/favor.png" Width="22" Height="22" ToolTip="{Binding Translation.Favor, FallbackValue=FAVOR}" />
                        <TextBlock Text="{Binding DashboardBindings.TotalGainedFavorInSession, Converter={StaticResource ShortNumberConverter}, ConverterCulture={x:Static glob:CultureInfo.CurrentCulture}, FallbackValue=0}" 
                                       FontSize="16" Foreground="{StaticResource SolidColorBrush.Text.1}" HorizontalAlignment="Center" />
                        <TextBlock FontSize="16" Text="{Binding DashboardBindings.FavorPerHour, Converter={StaticResource ValuePerHourConverter}, FallbackValue=0 /h}"
                           Foreground="{StaticResource SolidColorBrush.Text.1}" HorizontalAlignment="Center" />
                    </StackPanel>
                    <ItemsControl ItemsSource="{Binding FactionPointStats}" ItemTemplate="{StaticResource FactionPointStatsTemplate}" />
                </DockPanel>
            </Border>
        </Grid>

        <TabControl x:Name="SidebarTabControl" Style="{DynamicResource TabControlStyle}" SelectionChanged="SidebarTabControl_OnSelectionChanged">
            <TabItem Visibility="{Binding DashboardTabVisibility, Mode=OneWay}" Header="{Binding Translation.Dashboard, FallbackValue=DASHBOARD}" Tag="pack://application:,,,/Assets/Navigation/dashboard.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:DashboardControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>

            <TabItem Style="{StaticResource NavSectionHeaderStyle}" Header="{Binding Translation.NavCombat, FallbackValue=COMBAT}" Visibility="{Binding CombatGroupHeaderVisibility, Mode=OneWay}" />
            <TabItem Visibility="{Binding DamageMeterTabVisibility, Mode=OneWay}" Header="{Binding Translation.DamageMeter, FallbackValue=DAMAGE__METER}" Tag="pack://application:,,,/Assets/Navigation/bar-chart-rotated.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:DamageMeterControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>
            <TabItem Visibility="{Binding OpenWorldTabVisibility, Mode=OneWay}" Header="{Binding Translation.OpenWorld, FallbackValue=OPEN__WORLD}" Tag="pack://application:,,,/Assets/Navigation/open-world.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:OpenWorldControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>
            <TabItem Visibility="{Binding DungeonsTabVisibility, Mode=OneWay}" Header="{Binding Translation.Dungeons, FallbackValue=DUNGEONS}" Tag="pack://application:,,,/Assets/Navigation/dark.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:DungeonControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>
            <TabItem Style="{StaticResource NavSectionHeaderStyle}" Header="{Binding Translation.NavEconomy, FallbackValue=ECONOMY}" Visibility="{Binding EconomyGroupHeaderVisibility, Mode=OneWay}" />
            <TabItem Visibility="{Binding TradeMonitoringTabVisibility, Mode=OneWay}" Header="{Binding Translation.TradeMonitoring, FallbackValue=TRADE__MONITORING}" Tag="pack://application:,,,/Assets/Navigation/market.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:TradeMonitoringControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>
            <TabItem Visibility="{Binding CraftingTabVisibility, Mode=OneWay}" Header="{Binding Translation.Crafting, FallbackValue=CRAFTING}" Tag="pack://application:,,,/Assets/Navigation/anvil.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:CraftingControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>
            <TabItem Visibility="{Binding ArbitrageTabVisibility, Mode=OneWay}" Header="{Binding Translation.Arbitrage, FallbackValue=ARBITRAGE}" Tag="pack://application:,,,/Assets/Navigation/money.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:ArbitrageControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>
            <TabItem Visibility="{Binding GatheringTabVisibility, Mode=OneWay}" Header="{Binding Translation.Gathering, FallbackValue=GATHERING}" Tag="pack://application:,,,/Assets/Navigation/pickaxe.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:GatheringControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>
            <TabItem x:Name="ItemSearchTabItem" Visibility="{Binding ItemSearchTabVisibility, Mode=OneWay}" Header="{Binding Translation.ItemSearch, FallbackValue=ITEM__SEARCH}" Tag="pack://application:,,,/Assets/Navigation/magnifying-glass.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:ItemSearchControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>
            <TabItem Visibility="{Binding StorageHistoryTabVisibility, Mode=OneWay}" Header="{Binding Translation.StorageHistory, FallbackValue=STORAGE_HISTORY}" Tag="pack://application:,,,/Assets/Navigation/treasure.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:StorageHistoryControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>

            <TabItem Style="{StaticResource NavSectionHeaderStyle}" Header="{Binding Translation.NavTracking, FallbackValue=TRACKING}" Visibility="{Binding TrackingGroupHeaderVisibility, Mode=OneWay}" />
            <TabItem Visibility="{Binding LoggingTabVisibility, Mode=OneWay}" Header="{Binding Translation.Logging, FallbackValue=LOGGING}" Tag="pack://application:,,,/Assets/Navigation/log.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:LoggingControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>
            <TabItem Visibility="{Binding MapHistoryTabVisibility, Mode=OneWay}" Header="{Binding Translation.MapHistory, FallbackValue=MAP__HISTORY}" Tag="pack://application:,,,/Assets/Navigation/location.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:MapHistoryControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>
            <TabItem Visibility="{Binding PlayerInformationTabVisibility, Mode=OneWay}" Header="{Binding Translation.PlayerInformation, FallbackValue=PLAYER__INFORMATION}" Tag="pack://application:,,,/Assets/Navigation/baseball-player.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:PlayerInformationControl />
                </Grid>
            </TabItem>

            <TabItem Style="{StaticResource NavSectionHeaderStyle}" Header="{Binding Translation.NavSocial, FallbackValue=SOCIAL}" Visibility="{Binding SocialGroupHeaderVisibility, Mode=OneWay}" />
            <TabItem Visibility="{Binding PartyTabVisibility, Mode=OneWay}" Header="{Binding Translation.Party, FallbackValue=PARTY}" Tag="pack://application:,,,/Assets/Navigation/teamwork.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:PartyControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>
            <TabItem Visibility="{Binding GuildTabVisibility, Mode=OneWay}" Header="{Binding Translation.Guild, FallbackValue=GUILD}" Tag="pack://application:,,,/Assets/Navigation/shield.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:GuildControl DataContext="{Binding RelativeSource={RelativeSource AncestorType=Window}, Path=DataContext}" />
                </Grid>
            </TabItem>

            <TabItem Style="{StaticResource ItemContainerDonationStyle}" Header="{Binding Translation.Donations, FallbackValue=DONATIONS}" Tag="pack://application:,,,/Assets/Navigation/money.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:DonationControl />
                </Grid>
            </TabItem>
            <TabItem Style="{StaticResource ItemContainerThanksStyle}" Header="{Binding Translation.Thanks, FallbackValue=THANKS}" Tag="pack://application:,,,/Assets/heart.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:ThanksControl />
                </Grid>
            </TabItem>
            <TabItem x:Name="SettingsTabItem" Header="{Binding Translation.Settings, FallbackValue=SETTINGS}" Tag="pack://application:,,,/Assets/Navigation/setting.png">
                <Grid Style="{StaticResource TabItemGridStyle}">
                    <userControls:SettingsControl />
                </Grid>
            </TabItem>
        </TabControl>

        <userControls:FooterControl VerticalAlignment="Bottom" Height="46" />
        <StackPanel Orientation="Vertical" VerticalAlignment="Bottom">
            <userControls:ErrorBarControl VerticalAlignment="Bottom" Visibility="{Binding ErrorBarVisibility, FallbackValue=Collapsed}" ErrorText="{Binding ErrorBarText}" ErrorException="{Binding ErrorBarException}" />
            <userControls:WarningBarControl VerticalAlignment="Bottom" Visibility="{Binding WarningBarVisibility, FallbackValue=Collapsed}" WarningText="{Binding WarningBarText}" />
            <userControls:InformationBarControl VerticalAlignment="Bottom" Visibility="{Binding InformationBarVisibility, FallbackValue=Collapsed}" InformationText="{Binding InformationBarText}" />
        </StackPanel>
    </Grid>
</Window>
